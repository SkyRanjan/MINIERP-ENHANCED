name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install flake8
          flake8 app/ --count --max-line-length=120 --exclude=__pycache__ --statistics
      
      - name: Build Docker image
        run: docker build -t minierp-backend ./app
